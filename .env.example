# No API key needed — we use Ollama (local, free, open-source)
# Just make sure Ollama is running: https://ollama.com

# Model configuration (optional overrides)
OLLAMA_BASE_URL=http://localhost:11434
AGENT_MODEL=hermes3
TEXT_MODEL=qwen3
